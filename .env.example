# --- App (Next.js) Environment ---
# Cluster to use: devnet | mainnet | testnet | localnet
# If set to localnet/localhost, the header will default to devnet, but RPC can still point to localhost.
SOLANA_CLUSTER=devnet

# Optional public/client-side cluster override
NEXT_PUBLIC_SOLANA_CLUSTER=devnet

# RPC URL selection (set one or leave default behavior)
# For devnet
SOLANA_RPC_URL=https://api.devnet.solana.com
# Optional client-side RPC override
# NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# For local validator
# SOLANA_RPC_URL=http://localhost:8899
# NEXT_PUBLIC_SOLANA_RPC_URL=http://localhost:8899

# For mainnet
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# For testnet
# SOLANA_RPC_URL=https://api.testnet.solana.com
# NEXT_PUBLIC_SOLANA_RPC_URL=https://api.testnet.solana.com